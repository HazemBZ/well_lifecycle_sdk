export const projectName = "Well Lifecycle SDK";

// env var expansion with .env files is failing -> just move to vite later
const targetGateway = "localhost:8000";
export const targetServer = targetGateway;
